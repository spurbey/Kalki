{
  "name": "kalki",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "engines": {
    "node": ">=22.14.0"
  },
  "scripts": {
    "build": "npm run build -w @kalki/contracts && npm run build -w @kalki/server",
    "typecheck": "npm run build -w @kalki/contracts && npm run typecheck -w @kalki/server",
    "test": "npm run build -w @kalki/contracts && npm run test -w @kalki/contracts && npm run test -w @kalki/server",
    "format:check": "prettier --check .",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  }
}
